import { WholeDatePipe } from './whole-date.pipe';

describe('WholeDatePipe', () => {
  it('create an instance', () => {
    const pipe = new WholeDatePipe();
    expect(pipe).toBeTruthy();
  });
});
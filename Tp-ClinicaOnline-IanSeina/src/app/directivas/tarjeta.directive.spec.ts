import { TarjetaDirective } from './tarjeta.directive';

describe('TarjetaDirective', () => {
  it('should create an instance', () => {
    const directive = new TarjetaDirective();
    expect(directive).toBeTruthy();
  });
});
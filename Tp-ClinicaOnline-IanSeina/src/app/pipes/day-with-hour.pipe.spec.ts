import { DayWithHourPipe } from './day-with-hour.pipe';

describe('DayWithHourPipe', () => {
  it('create an instance', () => {
    const pipe = new DayWithHourPipe();
    expect(pipe).toBeTruthy();
  });
});